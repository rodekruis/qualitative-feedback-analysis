# Python API (folder index)

The section index for this folder is in [index.md](index.md). This README exists only as a landing page when you browse the folder on github.com — the canonical entry point is `index.md`.

The actual Python API reference is auto-generated by Sphinx and only appears in the built site (run `make docs` at the repo root). Browsing this folder on github.com will not show it.
