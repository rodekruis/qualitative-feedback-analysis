# Python API reference

Auto-generated reference for the `qfa` Python package — every public class, function, and module discovered under `src/qfa/`.

For the HTTP API that callers integrate against (endpoint shapes, error envelope, auth header), see the [REST API overview](../rest-api/index.md).

```{eval-rst}
.. autosummary::
   :toctree: _apidoc
   :recursive:

   qfa
```
